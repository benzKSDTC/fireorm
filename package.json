{
	"name": "fireorm",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"publish:major": "lerna publish major --conventional-commits --yes --tag-version-prefix='release@v'"
	},
	"devDependencies": {
		"lerna": "^3.16.1"
	}
}
